name: readme
description: 阅读Me
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  dio: ^4.0.0
  event_bus: ^2.0.0
  flustars: ^2.0.1
  html: ^0.15.0
  pull_to_refresh: ^2.0.0
  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
